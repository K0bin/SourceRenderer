{
  "name": "sourcerenderer-web-site",
  "version": "0.1.0",
  "description": "Render source engine maps in the browser",
  "main": "index.js",
  "scripts": {
    "build_ts": "tsc && tsc -p tsconfig.service_worker.json && tsc -p tsconfig.game_worker.json && tsc -p tsconfig.asset_worker.json",
    "build_rust": "cd ../lib && wasm-pack build -t no-modules -d ../www/dist/wasm_lib && cd ../www",
    "copy_src": "cp -r ./src/ ./dist/",
    "copy_static": "cp ./index.html ./dist/ && cp ./manifest.json ./dist/",
    "build": "npm run build_rust && npm run build_ts && npm run copy_static",
    "start": "npm run build && npm run copy_src && http-server ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/K0bin/SourceRenderer.git"
  },
  "keywords": [
    "webassembly",
    "wasm",
    "rust",
    "webpack"
  ],
  "author": "Robin Kertels <robin.kertels@gmail.com>",
  "license": "(MIT)",
  "bugs": {
    "url": "https://github.com/K0bin/SourceRenderer/issues"
  },
  "devDependencies": {
    "typescript": "^4.2.3",
    "http-server": "^0.12.3"
  }
}
