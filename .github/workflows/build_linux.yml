name: Rust

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install clippy
      run: rustup component add clippy
    - name: Build
      run: cd platform/sdl && cargo build --verbose
    - name: Lint
      run: cd platform/sdl && cargo clippy
#    - name: Run tests
#      run: cargo test --verbose
